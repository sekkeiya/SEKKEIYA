// src/features/layout/components/MainArea/components/scene/SceneGrid.jsx
import React from "react";
// import { Environment, Grid } from "@react-three/drei";

export default function SceneGrid() {
  return (
    <>
      {/* <Environment preset="city" />

      <Grid
        position={[0, 0, 0]}
        infiniteGrid
        sectionSize={1}
        sectionThickness={1}
        cellSize={0.5}
        cellThickness={0.5}
        fadeDistance={30}
      /> */}
    </>
  );
}
